import React from 'react';

const GallerySize = ({ size }) => (
  <div>
    <h1>GallerySize</h1>
    <span>
      {/* mountain logo */}
      [^`^] {size}
    </span>
  </div>
);

export default GallerySize;
